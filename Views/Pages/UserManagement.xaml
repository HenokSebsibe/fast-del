<UserControl x:Class="FoodyExpress.Views.Pages.UserManagement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="User Management" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextColor}"/>
            <StackPanel Orientation="Horizontal" Margin="0,10,0,10" HorizontalAlignment="Right">
                <Button x:Name="btnAddUser" Content="Add User" Width="100" Margin="5" Click="BtnAddUser_Click"/>
                <Button x:Name="btnEditUser" Content="Edit User" Width="100" Margin="5" Click="BtnEditUser_Click"/>
                <Button x:Name="btnDeleteUser" Content="Delete User" Width="100" Margin="5" Click="BtnDeleteUser_Click"/>
            </StackPanel>
            <DataGrid x:Name="dgUsers" AutoGenerateColumns="False" IsReadOnly="True" Height="350"
                      Background="{StaticResource SurfaceColor}" RowBackground="{StaticResource SurfaceColor}" AlternatingRowBackground="#FAFAFA"
                      HeadersVisibility="Column" SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding UserID}" Width="50"/>
                    <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="150"/>
                    <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
