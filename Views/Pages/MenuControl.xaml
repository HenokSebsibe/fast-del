<UserControl x:Class="FoodyExpress.Views.Pages.MenuControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Menu Items" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextColor}" Margin="0,0,0,15"/>
        
        <DataGrid x:Name="dgMenu" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" 
                  Background="{StaticResource SurfaceColor}" RowBackground="{StaticResource SurfaceColor}"
                  AlternatingRowBackground="#FAFAFA" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#E0E0E0"
                  BorderThickness="1" BorderBrush="#E0E0E0" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding MenuItemID}" Width="50"/>
                <DataGridTextColumn Header="Item Name" Binding="{Binding ItemName}" Width="200"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Price ($)" Binding="{Binding Price}" Width="100"/>
                <DataGridTextColumn Header="Available" Binding="{Binding IsAvailable}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
