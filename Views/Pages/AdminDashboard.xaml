<UserControl x:Class="FoodyExpress.Views.Pages.AdminDashboard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="Transparent">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Admin Dashboard" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextColor}" Margin="0,0,0,20"/>

        <UniformGrid Grid.Row="1" Columns="2" Rows="2" VerticalAlignment="Top" HorizontalAlignment="Stretch" Margin="0,10,0,0">
            <Border Style="{StaticResource GlassPanel}" Margin="10">
                <StackPanel>
                    <TextBlock Text="Total Users" FontSize="16" Foreground="{StaticResource TextColorLight}"/>
                    <TextBlock x:Name="TxtTotalUsers" Text="0" FontSize="32" FontWeight="Bold" Foreground="{StaticResource TextColor}" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource GlassPanel}" Margin="10">
                <StackPanel>
                    <TextBlock Text="Total Orders" FontSize="16" Foreground="{StaticResource TextColorLight}"/>
                    <TextBlock x:Name="TxtTotalOrders" Text="0" FontSize="32" FontWeight="Bold" Foreground="{StaticResource TextColor}" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource GlassPanel}" Margin="10">
                <StackPanel>
                    <TextBlock Text="Restaurants" FontSize="16" Foreground="{StaticResource TextColorLight}"/>
                    <TextBlock x:Name="TxtTotalRestaurants" Text="0" FontSize="32" FontWeight="Bold" Foreground="{StaticResource TextColor}" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource GlassPanel}" Margin="10">
                <StackPanel>
                    <TextBlock Text="Menu Items" FontSize="16" Foreground="{StaticResource TextColorLight}"/>
                    <TextBlock x:Name="TxtTotalMenuItems" Text="0" FontSize="32" FontWeight="Bold" Foreground="{StaticResource TextColor}" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </UniformGrid>
    </Grid>
</UserControl>
