<UserControl x:Class="FoodyExpress.Views.Pages.OrderManagement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="Transparent">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Order Management" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextColor}" Margin="0,0,0,20"/>

        <Border Grid.Row="1" Style="{StaticResource GlassPanel}" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Order ID:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox x:Name="TxtOrderId" Width="80" Margin="0,0,20,0"/>
                
                <TextBlock Text="New Status:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox x:Name="CmbStatus" Width="150" Margin="0,0,20,0">
                    <ComboBoxItem Content="Pending"/>
                    <ComboBoxItem Content="Preparing"/>
                    <ComboBoxItem Content="Out for Delivery"/>
                    <ComboBoxItem Content="Delivered"/>
                    <ComboBoxItem Content="Cancelled"/>
                </ComboBox>

                <Button Content="Update Status" Click="BtnUpdate_Click" Padding="15,5" Width="130"/>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="2" x:Name="DataGridOrders" AutoGenerateColumns="False" IsReadOnly="True" Margin="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Order ID" Binding="{Binding OrderID}" Width="80"/>
                <DataGridTextColumn Header="Customer ID" Binding="{Binding CustomerID}" Width="100"/>
                <DataGridTextColumn Header="Delivery Person ID" Binding="{Binding DeliveryPersonID}" Width="120"/>
                <DataGridTextColumn Header="Date" Binding="{Binding OrderDate}" Width="150"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120"/>
                <DataGridTextColumn Header="Total" Binding="{Binding TotalAmount, StringFormat=C}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
